header.header
    form(id="login-form", method="POST")
    .header__column
        ul
            li
                button.header-button 메뉴
            li
                a(href="/", style="width:100px, heigth:100px")
                    i.fas.fa-thumbs-up.fa-5x
    // Do Not Remove
    .header__column
        form(id="search-form", action="/results", method="GET")
            input(form="search-form", id="search-input", autocapitalize="none", autocomplete="off", autocorrect="off", name="search_query", type="text", spellcheck="false", placeholder="Search", value=search_query)    
              
    // Do Not Remove
    .header__column
        ul
            if login
                li.header_item
                        a(href="/video") MY VIDEO
                li.header_item
                        a(href="/video/upload") UPLOAD
                li.header_item
                        a(href="/user") MY CHANNEL
                        // /user에서 /user/:id로 jump
                li.header_item
                    input(form="login-form", type="submit", name="logout", value="LOGOUT")

            else
                li.header_item
                    input(form="login-form", type="submit", name="login", value="LOG IN")